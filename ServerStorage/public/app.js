const state={session:null,dashboard:null,editingId:null,needsSetup:false,banner:null};const app=document.getElementById("app");boot();
async function boot(){try{const setup=await api("/api/setup");state.needsSetup=setup.needsSetup;if(setup.needsSetup){render();return}state.session=await api("/api/session");if(state.session.authenticated){state.dashboard=await api("/api/scripts")}render()}catch(error){state.banner={type:"error",message:error.message};render()}}
function render(){if(state.needsSetup){app.innerHTML=renderSetup();bindSetup();return}if(!state.session?.authenticated){app.innerHTML=renderLogin();bindLogin();return}app.innerHTML=renderDashboard();bindDashboard()}
function renderSetup(){return `<section class="auth-shell">${renderBanner()}<div class="brand"><div class="brand-mark">SS</div><div class="brand-copy"><h1>ServerStorage</h1><p>Single-user script vault</p></div></div><h2 class="auth-title">Create your owner password</h2><p class="auth-copy">This only happens once. Your password is stored as a strong hash locally on the server.</p><form id="setup-form" class="form-grid"><div class="field"><label for="setup-password">Password</label><input id="setup-password" name="password" type="password" minlength="12" required /></div><button class="primary-button" type="submit">Finish setup</button></form></section>`}
function renderLogin(){return `<section class="auth-shell">${renderBanner()}<div class="brand"><div class="brand-mark">SS</div><div class="brand-copy"><h1>ServerStorage</h1><p>Private Lua hosting dashboard</p></div></div><h2 class="auth-title">Sign in</h2><p class="auth-copy">Use your owner password to manage hosted scripts and raw endpoints.</p><form id="login-form" class="form-grid"><div class="field"><label for="login-password">Password</label><input id="login-password" name="password" type="password" required /></div><button class="primary-button" type="submit">Unlock dashboard</button></form></section>`}
function renderDashboard(){const stats=state.dashboard?.stats||{totalScripts:0,enabled:0,executions:0,successRate:0,failed:0};const scripts=state.dashboard?.scripts||[];const editing=scripts.find(script=>script.id===state.editingId)||null;return `<main class="app-shell"><header class="topbar"><div class="brand"><div class="brand-mark">SS</div><div class="brand-copy"><h1>ServerStorage</h1><p>Private hosting for your Lua files</p></div></div><div class="nav-row"><span class="nav-link">Scripts</span><span class="nav-link">Vault</span><span class="pill">Single Owner</span><button class="ghost-button" id="logout-button" type="button">Logout</button></div></header><section class="layout"><div class="stack"><section class="hero">${renderBanner()}<div class="hero-top"><div><div class="title-row"><span class="status-dot"></span><h2>Script Control</h2><span class="chip live">owner mode</span></div><p class="subtle">Secure personal storage and raw hosting links for your Roblox workflow.</p></div><div class="hero-actions"><button class="ghost-button" id="new-script-button" type="button">New Script</button><button class="primary-button" id="refresh-button" type="button">Refresh</button></div></div><div class="hero-grid"><article class="stat-card"><div class="stat-label">Total Scripts</div><div class="card-value">${stats.totalScripts}</div><div class="hero-note">Stored in your private vault</div></article><article class="stat-card purple"><div class="stat-label">Enabled</div><div class="card-value">${stats.enabled}</div><div class="hero-note">Raw routes currently active</div></article><article class="stat-card green"><div class="stat-label">Executions</div><div class="card-value">${stats.executions}</div><div class="hero-note">Tracked successful raw requests</div></article><article class="stat-card red"><div class="stat-label">Failed</div><div class="card-value">${stats.failed}</div><div class="hero-note">Disabled or invalid token requests</div></article></div></section><section class="panel script-panel"><div class="section-head"><div><h3 class="section-title">${editing?"Edit Script":"Upload Script"}</h3><p class="helper">Paste Lua code, save it, then use the generated raw URL in your game.</p></div></div><form id="script-form" class="form-grid"><input type="hidden" name="id" value="${editing?.id||""}" /><div class="field"><label for="script-name">Script name</label><input id="script-name" name="name" value="${escapeHtml(editing?.name||"")}" required /></div><div class="field"><label for="script-description">Description</label><input id="script-description" name="description" value="${escapeHtml(editing?.description||"")}" /></div><div class="field"><div class="label-row"><label for="script-code">Lua source</label><span class="helper">Stored server-side, not in the browser</span></div><textarea id="script-code" name="code" spellcheck="false" required>${escapeHtml(editing?.code||"-- Paste your Lua script here")}</textarea></div><label class="toggle"><input id="script-enabled" name="enabled" type="checkbox" ${(editing?.enabled ?? true)?"checked":""} />Raw endpoint enabled</label><div class="actions-row"><button class="primary-button" type="submit">${editing?"Save Changes":"Create Script"}</button>${editing?'<button class="ghost-button" id="cancel-edit-button" type="button">Cancel</button>':""}</div></form></section><section class="panel"><div class="section-head"><div><h3 class="section-title">Hosted Scripts</h3><p class="helper">Every raw endpoint is protected with a per-script secret token.</p></div></div><div class="script-list">${scripts.length?scripts.map(renderScriptCard).join(""):'<div class="empty-state">No scripts yet. Create your first one from the editor above.</div>'}</div></section></div><aside class="stack"><section class="panel"><div class="section-head"><h3 class="section-title">Success Rate</h3></div><div class="meter-wrap"><div class="ring" style="--progress:${Math.max(stats.successRate,2)}%"><strong>${stats.successRate}%</strong></div><div><div>${Math.max(stats.executions-stats.failed,0)} ok / ${stats.failed} fail</div><div class="meter-bar"><span style="width:${stats.successRate}%"></span></div></div></div></section><section class="panel"><div class="section-head"><h3 class="section-title">Quick Actions</h3></div><div class="action-list"><div class="action-item"><div class="action-icon">+</div><div><div>Upload script</div><div class="subtle">Save fresh Lua source</div></div></div><div class="action-item"><div class="action-icon">R</div><div><div>Rotate tokens</div><div class="subtle">Invalidate exposed raw links fast</div></div></div><div class="action-item"><div class="action-icon">S</div><div><div>Single-user security</div><div class="subtle">Password gate and strict cookies</div></div></div></div></section><section class="panel"><div class="section-head"><h3 class="section-title">Usage Snippet</h3></div><div class="code-inline">loadstring(game:HttpGet("raw-url-here"))()</div><p class="helper">Replace the URL with the generated raw link below. Keep those links private because the token grants access.</p></section></aside></section></main>`}
function renderScriptCard(script){return `<article class="script-item" data-script-id="${script.id}"><div class="script-row"><div><div class="script-name">${escapeHtml(script.name)}</div><div class="script-meta">${escapeHtml(script.description||"No description")} - updated ${formatDate(script.updatedAt)}</div></div><span class="chip ${script.enabled?"live":"offline"}">${script.enabled?"enabled":"disabled"}</span></div><div class="script-links"><div class="code-inline">${escapeHtml(script.rawUrl)}</div><div class="subtle">${escapeHtml(`loadstring(game:HttpGet("${script.rawUrl}"))()`)}</div></div><div class="script-actions"><button class="ghost-button" type="button" data-action="copy-url">Copy URL</button><button class="ghost-button" type="button" data-action="copy-loadstring">Copy Loadstring</button><button class="ghost-button" type="button" data-action="edit">Edit</button><button class="ghost-button" type="button" data-action="rotate">Rotate Token</button><button class="danger-button" type="button" data-action="delete">Delete</button></div></article>`}
function renderBanner(){return state.banner?`<div class="banner ${state.banner.type}">${escapeHtml(state.banner.message)}</div>`:""}
function bindSetup(){document.getElementById("setup-form")?.addEventListener("submit",async event=>{event.preventDefault();const form=new FormData(event.currentTarget);await submit(async()=>{await api("/api/setup",{method:"POST",body:{password:form.get("password")}});state.needsSetup=false;state.session={authenticated:false};state.banner={type:"success",message:"Setup complete. Sign in with your new password."}})})}
function bindLogin(){document.getElementById("login-form")?.addEventListener("submit",async event=>{event.preventDefault();const form=new FormData(event.currentTarget);await submit(async()=>{await api("/api/login",{method:"POST",body:{password:form.get("password")}});state.session=await api("/api/session");state.dashboard=await api("/api/scripts");state.banner={type:"success",message:"Dashboard unlocked."}})})}
function bindDashboard(){document.getElementById("logout-button")?.addEventListener("click",async()=>{await submit(async()=>{await api("/api/logout",{method:"POST"});state.session={authenticated:false};state.dashboard=null;state.editingId=null;state.banner={type:"success",message:"Logged out."}})});document.getElementById("refresh-button")?.addEventListener("click",refreshDashboard);document.getElementById("new-script-button")?.addEventListener("click",()=>{state.editingId=null;state.banner=null;render()});document.getElementById("cancel-edit-button")?.addEventListener("click",()=>{state.editingId=null;render()});document.getElementById("script-form")?.addEventListener("submit",async event=>{event.preventDefault();const form=new FormData(event.currentTarget);const payload={name:form.get("name"),description:form.get("description"),code:form.get("code"),enabled:form.get("enabled")==="on"};const id=form.get("id");await submit(async()=>{if(id){await api(`/api/scripts/${id}`,{method:"PUT",body:payload,csrf:true});state.banner={type:"success",message:"Script updated."}}else{await api("/api/scripts",{method:"POST",body:payload,csrf:true});state.banner={type:"success",message:"Script created."}}state.editingId=null;await refreshDashboard()})});document.querySelectorAll("[data-script-id]").forEach(card=>{card.addEventListener("click",async event=>{const button=event.target.closest("button[data-action]");if(!button)return;const scriptId=card.dataset.scriptId;const action=button.dataset.action;const script=state.dashboard.scripts.find(item=>item.id===scriptId);if(!script)return;if(action==="edit"){state.editingId=scriptId;state.banner=null;render();return}if(action==="copy-url"){await navigator.clipboard.writeText(script.rawUrl);state.banner={type:"success",message:"Raw URL copied."};render();return}if(action==="copy-loadstring"){await navigator.clipboard.writeText(`loadstring(game:HttpGet("${script.rawUrl}"))()`);state.banner={type:"success",message:"Loadstring copied."};render();return}if(action==="rotate"){await submit(async()=>{await api(`/api/scripts/${scriptId}/rotate-token`,{method:"POST",csrf:true});state.banner={type:"success",message:"Token rotated."};await refreshDashboard()});return}if(action==="delete"){if(!window.confirm(`Delete ${script.name}?`))return;await submit(async()=>{await api(`/api/scripts/${scriptId}`,{method:"DELETE",csrf:true});state.banner={type:"success",message:"Script deleted."};if(state.editingId===scriptId)state.editingId=null;await refreshDashboard()})}})})}
async function refreshDashboard(){state.dashboard=await api("/api/scripts");render()}
async function submit(task){try{state.banner=null;await task();render()}catch(error){state.banner={type:"error",message:error.message};render()}}
async function api(url,options={}){const response=await fetch(url,{method:options.method||"GET",headers:{"Content-Type":"application/json",...(options.csrf&&state.session?.csrfToken?{"X-CSRF-Token":state.session.csrfToken}:{})},body:options.body?JSON.stringify(options.body):undefined});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||"Request failed.");return payload}
function formatDate(value){return new Date(value).toLocaleString(undefined,{year:"numeric",month:"short",day:"numeric"})}
function escapeHtml(value){return String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#39;")}
